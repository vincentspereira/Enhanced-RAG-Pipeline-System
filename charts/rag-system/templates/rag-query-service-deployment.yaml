{{- if .Values.ragQueryService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rag-system.componentFullname" (dict "component" .Values.ragQueryService "context" .) }}
  labels:
    {{- include "rag-system.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.ragQueryService.name }}
spec:
  replicas: {{ .Values.replicaCount }} # Global or .Values.ragQueryService.replicaCount
  selector:
    matchLabels:
      {{- include "rag-system.selectorLabelsComponent" (dict "componentName" .Values.ragQueryService.name "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "rag-system.selectorLabelsComponent" (dict "componentName" .Values.ragQueryService.name "context" .) | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "rag-system.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Values.ragQueryService.name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/app/entrypoint.sh"]
          args: ["rag_query_service"] # Specific argument for this service
          ports:
            - name: http
              containerPort: {{ .Values.ragQueryService.containerPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "rag-system.componentFullname" (dict "component" .Values.ragQueryService "context" .) }}-config
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 60 # Increased due to model loading
            periodSeconds: 30
            timeoutSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30 # Increased
            periodSeconds: 20
            timeoutSeconds: 10
          resources:
            {{- toYaml .Values.ragQueryService.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
