{{- if .Values.internalApiGateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rag-system.componentFullname" (dict "component" .Values.internalApiGateway "context" .) }}
  labels:
    {{- include "rag-system.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.internalApiGateway.name }}
spec:
  replicas: {{ .Values.replicaCount }} # Global replicaCount, or specific .Values.internalApiGateway.replicaCount
  selector:
    matchLabels:
      {{- include "rag-system.selectorLabelsComponent" (dict "componentName" .Values.internalApiGateway.name "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "rag-system.selectorLabelsComponent" (dict "componentName" .Values.internalApiGateway.name "context" .) | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "rag-system.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Values.internalApiGateway.name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/app/entrypoint.sh"]
          args: ["internal_api_gateway"] # Specific argument for this service
          ports:
            - name: http
              containerPort: {{ .Values.internalApiGateway.containerPort }}
              protocol: TCP
          env:
            - name: RAG_QUERY_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "rag-system.componentFullname" (dict "component" .Values.internalApiGateway "context" .) }}-config
                  key: RAG_QUERY_SERVICE_URL
            - name: DOC_PROCESSING_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "rag-system.componentFullname" (dict "component" .Values.internalApiGateway "context" .) }}-config
                  key: DOC_PROCESSING_SERVICE_URL
            - name: API_GATEWAY_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "rag-system.componentFullname" (dict "component" .Values.internalApiGateway "context" .) }}-config
                  key: API_GATEWAY_PORT
            - name: LOGGING_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "rag-system.componentFullname" (dict "component" .Values.internalApiGateway "context" .) }}-config
                  key: LOGGING_LEVEL
            - name: VALID_API_KEYS_CSV
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.internalApiGateway.apiKeys.existingSecret.name | default (printf "%s-apikeys" (include "rag-system.componentFullname" (dict "component" .Values.internalApiGateway "context" .))) }}
                  key: {{ .Values.internalApiGateway.apiKeys.existingSecret.keyName | default "api-keys-csv" }}
                  optional: {{ not (or .Values.internalApiGateway.apiKeys.existingSecret.name .Values.internalApiGateway.apiKeys.createSecret) }} # Optional if neither existing nor create is set
          livenessProbe:
            httpGet:
              path: /gateway_health # Ensure this path exists in your gateway app
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /gateway_health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.internalApiGateway.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
